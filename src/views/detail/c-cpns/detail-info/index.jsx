import React, { memo } from 'react'
import { InfoWrapper } from './sytle'

const DetailInfos = memo((props) => {
  return (
    <InfoWrapper>
    </InfoWrapper>
  )
})

DetailInfos.propTypes = {}

export default DetailInfos